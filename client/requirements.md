## Packages
framer-motion | Smooth animations for hero sections, product cards, and page transitions
lucide-react | Icon system for UI elements (already in base, but emphasizing usage)

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  serif: ["'Playfair Display'", "serif"],
  sans: ["'DM Sans'", "sans-serif"],
}
